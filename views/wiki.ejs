<% include header %>
<ul class="breadcrumb">
    <li>
        <a href="/">Wiki</a>
        <span class="divider">/</span>
    </li>
    <% page.breadCrumbs.forEach(function(crumb){ %>
    <li>
        <a href="/wiki<%= crumb.url %>"><%= crumb.title %></a>
        <span class="divider">/</span>
    </li>
    <% }); %>
    <div class="btn-group wiki-tools pull-right btn-group-wiki-controls">
        <a class="btn btn-wiki-add-child" href="#" title="<%= i18n.addSubPage %>"><i class="icon-plus"></i></a>
        <a class="btn btn-wiki-edit" data-act="edit" href="#" title="<%= i18n.Edit %>"><i class="icon-pencil"></i></a>
        <a class="btn btn-wiki-remove" title="<%= i18n.Remove %>" href="#"><i class="icon-remove"></i></a>
    </div>
</ul>
<div class="container-fluid body">
    <div class="row-fluid">
        <div class="span12 wiki-article">

            <div class="wiki-article-text">
                <%- page.text %>
            </div>
             <% if (page.children.length > 0) { %>
                <div class ="wiki-children">
                    <h5><%= i18n.subPages %></h5>
                    <% page.children.forEach(function(child) { %>
                        <a href="/wiki/<%= page.currentUrl %><%= child %>"><%= child %></a><br>
                    <% }); %>
                </div>
            <% } %>
            <% if (page.files.length > 0) { %>
                <div class ="wiki-children-files">
                    <h5><%= i18n.attachedFiles %></h5>
                    <% page.files.forEach(function(file) { %>
    <div class="btn-group">
        <a class="btn btn-mini dropdown-toggle" data-toggle="dropdown" href="#">
            <% if (file.type=='file') { %>
            <%= file.name %>
            <% } else { %>
            <img width="60" src="/wiki/<%= page.currentUrl %><%= file.name %>" />
            <% } %>
        <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
        <li><a class="wiki-remove-file" href="/remove/<%= page.currentUrl %><%= file.name %>"><%= i18n.Remove %></a></li>
        <li><a href="/wiki/<%= page.currentUrl %><%= file.name %>"><%= file.name %></a></li>
        </ul>
    </div>
                    <% }); %>
                </div>
            <% } %>
        </div>
    </div>
    <form class="wiki-editor">
    <a class="btn btn-wiki-edit btn-success" data-act="save" href="#">
        <i class="icon-ok icon-white"></i> <%= i18n.Save %>
    </a>
    <a class="btn btn-wiki-dont-edit btn-danger" href="#">
        <%= i18n.dontSave %>
    </a>
    <br>
    <br>
        <input name="header" class="span12 wiki-header-edit" type="text" value="<%= page.header %>">
        <textarea name="text" class="span12"><%= page.clearText %></textarea>
    </form>
    <form class="wiki-new-page">
        <a class="btn btn-wiki-add-child  btn-primary" href="#"><%= i18n.Add %></a>
        <a class="btn btn-danger btn-wiki-dont-add-child" href="#"><%= i18n.dontAdd %></a>
        <br><br>
        <input name="header" class="span12 wiki-header-edit" type="text" placeholder="<%= i18n.Header %>">
        <textarea name="text" class="span12" placeholder="<%= i18n.someText %>"></textarea>
    </form>
</div>
<div class="well wiki-stuff">
    <form action="/upload" method="post" enctype="multipart/form-data">
        <input type="file" name="file">
        <input type="hidden" name="referer" value="<%= page.currentUrl %>">
        <input type="submit" class="btn btn-mini" value="<%= i18n.attacheFiles %>">
        <a href="#" class="btn btn-mini"><%= i18n.History %></a>
    </form>
</div>
<% include footer %>